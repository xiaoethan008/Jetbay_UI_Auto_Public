# 历史继承规则

本文件记录已经被产品或测试口径确认、并且会跨版本继续生效的规则。新版本需求文档不是孤立来源；除非新版本明确说明并经产品/用户确认覆盖旧规则，否则本文件中的规则默认继续有效。

“后续版本”按代码分支继承关系判断，不只按版本号大小判断。两个版本若从同一主分支并行拉取，后创建的规则不能自动应用到另一条并行分支；只有相关需求或实现已合入其共同基线，或产品明确要求同步时，才作为该版本的验收规则。

## 使用规则

每个新版本开测前必须先读取本文件，并把适用规则写入需求追溯矩阵。

写入矩阵前必须核对目标版本的分支起点、合并记录和需求关系。来源版本与目标版本为并行分支时，规则标记为“不适用/未继承”，不得据此提交目标版本缺陷。

若最新需求文档、截图、表格或代码实现与历史继承规则冲突：

1. 先记录为 `文档冲突 / 需求遗漏` 或 `待确认`。
2. 不静默忽略冲突。
3. 不因为页面遵循历史规则而直接报产品缺陷。
4. 只有产品/用户明确确认“本版本覆盖旧规则”时，才更新本文件并按新规则验收。

## 矩阵字段建议

| 字段 | 说明 |
| --- | --- |
| 继承规则 | 关联本文件中的规则 ID |
| 是否被本版本覆盖 | 是 / 否 / 待确认 |
| 冲突判断 | 无冲突 / 文档冲突 / 截图冲突 / 代码实现冲突 |
| 处理口径 | 按历史规则验收 / 按新需求验收 / 待产品确认 |

## 当前继承规则

| 规则 ID | 来源版本 | 适用范围 | 规则 | 后续处理 |
| --- | --- | --- | --- | --- |
| INHERIT-BRAND-001 | V4.1.1 全站品牌词 | 官网可见正文、菜单、CTA、核心模块文案 | 页面可见品牌词统一使用 `Jetbay`，不使用全大写 `JETBAY`；需求文档中残留 `JETBAY` 视为需求文档遗漏，除非产品明确确认本版本要恢复大写。 | 后续版本 PRD 如再次出现 `JETBAY Membership`、`JETBAY Travel Credit` 等写法，先记录为 `文档冲突 / 需求遗漏`；页面按 `Jetbay` 展示不报产品缺陷。 |
| INHERIT-POLLING-001 | V4.1.3 搜索结果轮询 | Search、SOS 结果页日期过期校验 | 搜索结果是否过期由后端根据当前时间判断；前端请求提供 `cityId`、`selectedDate` 即符合设计，不要求前端传搜索结果生成时间。 | 后续验证重点检查后端业务结果、10 分钟轮询、失败重试和停止条件；不得因前端请求缺少结果生成时间直接报缺陷。 |
| INHERIT-POLLING-002 | V4.1.3 后台标签页轮询 | Search、SOS 结果页 | 后台标签页的定时器暂停或延迟由浏览器控制，前端不强制监听 `visibilitychange`；返回前台时不立即补做日期校验，继续等待原轮询周期。返回 `23103` 时停止轮询并显示过期弹窗。 | 后续版本不得把“后台仍继续请求”或“返回前台未立即校验”直接判为缺陷；只需验证前台正常 10 分钟轮询、`23103` 停止及弹窗。 |
| INHERIT-LEAD-TIME-001 | V4.1.3 线索客户端时间字段 | 普通线索、机场线索、Empty Leg 线索提交 | 前端请求统一携带格式为 `yyyy-MM-dd HH:mm:ss` 的设备本地时间 `clientSubmitTime`，以及 IANA 时区 `clientTimezone`；后端按请求原值保存至 `client_submit_time`、`client_timezone`。 | 后续版本回归至少抓取一个真实页面线索请求校验两字段存在且格式正确；具备数据库连接时抽查落库值。 |
| INHERIT-DEPARTURE-MODAL-001 | V4.1.3 日期过期兜底 | Search、SOS、直接线索表单 | 后端返回 `23103/success=false` 时只显示一层日期不可用弹窗；点击日期选择 CTA 只关闭弹窗，不自动清空旧结果或重新搜索；不得同时显示重复错误 toast。 | 后续版本覆盖弹窗单实例、一次关闭、无重复 toast；文案按对应版本需求验收。 |

## 补充规则流程

发现新的跨版本规则后：

1. 在当前版本问题清单中说明来源和用户/产品确认口径。
2. 在本文件新增一行规则，给出规则 ID、来源版本和适用范围。
3. 若可自动化，补到固定回归或专项脚本索引；若不可自动化，写明人工检查方式。
4. 下个版本开测时，把规则写入需求追溯矩阵并检查新 PRD 是否冲突。

## 版本收尾检查

每个版本测试收尾时，必须回看以下材料，判断是否有需要沉淀为跨版本继承规则的内容：

- 当前版本问题清单
- 未测 / 阻塞项
- 待确认 / 部分覆盖项
- 文档冲突 / 需求遗漏项
- 用户或产品在对话中明确确认的验收口径
- 因需求变更关闭、忽略或降级的问题

满足以下任一条件时，应补充到本文件：

1. 该规则后续版本仍默认适用。
2. 该规则用于避免把历史需求误带入新版本。
3. 该规则用于避免把新文档遗漏误报为页面缺陷。
4. 该规则影响固定回归、问题分类或发布判断。

若确认没有需要继承的规则，也要在版本总结或问题清单中写明“本轮无新增历史继承规则”。
